/* =============================================================
   SexEyeUp — Products Data
   The PRODUCTS constant is injected by the server via the layout.
   This file is kept for compatibility only.
   ============================================================= */
if (typeof PRODUCTS === 'undefined') {
    // fallback empty array if the server did not inject products
    var PRODUCTS = [];
}

